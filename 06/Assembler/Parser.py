## strip white space

## grab each line

    ## parse through the code

    ## no ; means computation

    ## ; and JMP means computation and JMP

    ## = and no ; means dest=comp

    ## = and ; means dest=computation then JMP

    ##